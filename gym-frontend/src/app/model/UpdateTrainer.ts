export class UpdateTrainer {
    constructor(private username: string,
        private firstname: string,
        private lastname: string,
        private active: boolean,private specialization:string) { }
}